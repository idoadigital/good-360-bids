# Top-level direct dependencies (human-edited).
# Regenerate the pinned lockfile:
#   pip install pip-tools
#   pip-compile --generate-hashes --output-file=requirements.txt requirements.in

# Runtime
cryptography>=42,<46
flask>=3.0,<4
playwright==1.58.0
python-telegram-bot==22.7
pytz>=2024.1
requests>=2.32,<3
openai-agents>=0.14,<1
pydantic>=2.13,<3

# Config / env
python-dotenv>=1.0,<2

# Transitive deps the postmortem showed were missing on container restart
httpx>=0.28,<1
aiohttp>=3.9,<4
