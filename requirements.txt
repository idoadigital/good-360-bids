# PINNED runtime requirements. For a true transitive lockfile run:
#   pip install pip-tools
#   pip-compile --generate-hashes --output-file=requirements.txt requirements.in
# First pass — covers every top-level import in the codebase plus the two
# transitive deps (httpx, aiohttp) flagged by the April 13 outage postmortem.
aiohttp==3.11.11
cryptography==44.0.2
flask==3.1.2
httpx==0.28.1
playwright==1.58.0
python-dotenv==1.1.1
python-telegram-bot==22.7
pytz==2026.1.post1
requests==2.32.5
openai-agents==0.14.2
pydantic==2.13.3
